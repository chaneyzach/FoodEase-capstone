@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    <link href="~/Content/MealPlanner/AddRecipe.css" rel="stylesheet" type="text/css" />
    <script src="~/Scripts/views/addrecipe.js"></script>
}
@using Capstone.Web.Models.ViewModels
@model RecipeDetailViewModel

<body>
    <!-- START Site Preloader -->
    <div id="theme-preloader-container">
        <div class="theme-preloader">
            <span class="circle-01"></span>
            <span class="circle-02"></span>
            <span class="circle-03"></span>
        </div>
    </div>
    <!-- END Site Preloader -->
    <div class="container">
        <div class="row-recipe" id="register">
            <div class="col-sm-12">
                @using (Html.BeginForm("ModifyRecipe", "Recipe", FormMethod.Post, new { id = "recipeForm", enctype = "multipart/form-data" }))
                {
                    <div class="row">
                        <div class="col-sm-6 ing_list">
                            <p>Update Recipe Details:</p>
                            <button type="submit" class="btn btn-default add">Save Recipe</button>
                            <div class="form-group col-sm-12">
                                @Html.TextBox("Id", "" + Model.Recipe.Id + "", new { type = "hidden" })
                            </div>
                            <div class="form-group col-sm-12">
                                @Html.Label("Name")
                                @Html.TextBox("Name", "" + Model.Recipe.Name + "", new { @class = "form-control" })
                                @Html.ValidationMessage("Name", new { @class = "error" })
                            </div>
                            <div class="form-group col-sm-12">
                                @Html.Label("Description")
                                @Html.TextArea("Description", "" + Model.Recipe.Description + "", new { @class = "form-control" })
                                @Html.ValidationMessage("Description", new { @class = "error" })
                            </div>
                            <div class="form-group col-sm-12">
                                @Html.Label("Instructions")
                                @Html.TextArea("Instructions", "" + Model.Recipe.Instructions + "", new { @class = "form-control" })
                                @Html.ValidationMessage("Instructions", new { @class = "error" })
                            </div>
                            <div>
                                <div class="form-group col-sm-5 recipeTimeLeft">
                                    @Html.Label("Prep Time (minutes)")
                                    @Html.TextBox("PrepTime", "" + Model.Recipe.PrepTime + "", new { @class = "form-control" })
                                    @Html.ValidationMessage("PrepTime", new { @class = "error" })
                                </div>
                                <div class="form-group col-sm-5 recipeTimeRight">
                                    @Html.Label("Cook Time (minutes)")
                                    @Html.TextBox("CookTime", "" + Model.Recipe.CookTime + "", new { @class = "form-control" })
                                    @Html.ValidationMessage("CookTime", new { @class = "error" })
                                </div>
                            </div>
                            <div class="form-group col-sm-12">
                                @Html.Label("Food Image")
                                @Html.TextBox("FoodImage", null, new { @class = "form-control-file", type = "file" })
                                @Html.ValidationMessage("FoodImage")
                            </div>
                        </div>

                        <div class="col-sm-6 ing_list">
                            <p>Ingredients:</p>
                            <button type="button" id="add_ing" class="btn btn-default add">Add Another</button>
                            <div class="ingredient_box">
                                <div class="form-group col-sm-3" style="float: left;">
                                    @Html.Label("Quantity")
                                    @Html.TextBox("quantities", "", new { @class = "form-control", placeholder = "Qty" })
                                    @Html.ValidationMessage("Quantity", new { @class = "error" })
                                </div>
                                <div class="form-group col-sm-9" style="float: left;">
                                    @Html.Label("Ingredient")
                                    @Html.TextBox("ingredients", "", new { @class = "form-control", placeholder = "Ingredient" })
                                    @Html.ValidationMessage("Ingredient", new { @class = "error" })
                                </div>
                            </div>
                            @for (int i = 0; i < Model.Quantities.Count; i++)
                            {
                                <div>
                                    <div class="form-group col-sm-3" style="float: left;">
                                        @Html.Label("Quantity")
                                        @Html.TextBox("quantities", "" + Model.Quantities[i] + "", new { @class = "form-control", placeholder = "Quantity" })
                                        @Html.ValidationMessage("Quantity", new { @class = "error" })
                                    </div>
                                    <div class="form-group col-sm-9" style="float: left;">
                                        @Html.Label("Ingredient")
                                        @Html.TextBox("ingredients", "" + Model.Ingredients[i].Name + "", new { @class = "form-control", placeholder = "Ingredient" })
                                        @Html.ValidationMessage("Ingredient", new { @class = "error" })
                                    </div>
                                </div>
                            }
                            @*<div class="form-group col-sm-2" style="float: left;">
                                    <button type="button" id="add_ing" class="btn btn-default">Add</button>
                                </div>*@

                            @*<button>Update Recipe</button>*@
                        </div>
                    </div>
                }
            </div>
            <div class="col-sm-4"></div>
        </div>
    </div>
    <!-- Theme Base, Components and Settings -->
    <script src="~/Scripts/theme.js"></script>
</body>